ubuntu16.04

```
$ docker build -t test .
$ docker run test                             
return_value=3
No matches found or an error occurred.
```

ubuntu18.04

```
$ docker build -t test .
$ docker run test
return_value=0
test.so
```